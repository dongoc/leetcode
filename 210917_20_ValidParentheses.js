// https://leetcode.com/problems/valid-parentheses/

/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
  const bracketPair = {
      '(': ')',
      '{': '}',
      '[': ']'
  }

  const stack = []

  for (let i = 0; i < s.length; i++) {
      // if ()
  }
};
